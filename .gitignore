__pycache__/
*.pyc
.venv/
venv/
.DS_Store
*.egg-info/

# Per-repo local settings written by tools/assign_slot.py — contains the
# anti-spoofing token (daemon/auth.py), never commit this.
.claude/settings.local.json

# SwiftUI widget build artifacts (widget/build.sh output + SPM's own cache).
widget/.build/
widget/build/
widget/.swiftpm/
